# Git + Solidity Assessment

## Objective
Fix and extend the Token smart contract so that all tests pass.

## Requirements
- Fix existing issues in the contract
- Implement missing features
- Ensure all tests pass

## Setup
```bash
npm install
npx hardhat test